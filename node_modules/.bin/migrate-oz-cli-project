#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@openzeppelin/truffle-upgrades/dist/scripts/migrate-oz-cli-project.js" "$@"
  ret=$?
else 
  node  "$basedir/../@openzeppelin/truffle-upgrades/dist/scripts/migrate-oz-cli-project.js" "$@"
  ret=$?
fi
exit $ret
