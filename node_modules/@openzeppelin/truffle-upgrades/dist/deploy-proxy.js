"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deployProxy = void 0;
const upgrades_core_1 = require("@openzeppelin/upgrades-core");
const deploy_proxy_admin_1 = require("./deploy-proxy-admin");
const utils_1 = require("./utils");
const initializer_data_1 = require("./utils/initializer-data");
async function deployProxy(Contract, args = [], opts = {}) {
    if (!Array.isArray(args)) {
        opts = args;
        args = [];
    }
    const { deployer } = (0, utils_1.withDefaults)(opts);
    const provider = (0, utils_1.wrapProvider)(deployer.provider);
    const manifest = await upgrades_core_1.Manifest.forNetwork(provider);
    const { impl, kind } = await (0, utils_1.deployProxyImpl)(Contract, opts);
    const data = (0, initializer_data_1.getInitializerData)(Contract, args, opts.initializer);
    if (kind === 'uups') {
        if (await manifest.getAdmin()) {
            (0, upgrades_core_1.logWarning)(`A proxy admin was previously deployed on this network`, [
                `This is not natively used with the current kind of proxy ('uups').`,
                `Changes to the admin will have no effect on this new proxy.`,
            ]);
        }
    }
    let proxyDeployment;
    switch (kind) {
        case 'beacon': {
            throw new upgrades_core_1.BeaconProxyUnsupportedError();
        }
        case 'uups': {
            const ProxyFactory = (0, utils_1.getProxyFactory)(Contract);
            proxyDeployment = Object.assign({ kind }, await (0, utils_1.deploy)(deployer, opts, ProxyFactory, impl, data));
            break;
        }
        case 'transparent': {
            const adminAddress = await (0, deploy_proxy_admin_1.deployProxyAdmin)(opts);
            const TransparentUpgradeableProxyFactory = (0, utils_1.getTransparentUpgradeableProxyFactory)(Contract);
            proxyDeployment = Object.assign({ kind }, await (0, utils_1.deploy)(deployer, opts, TransparentUpgradeableProxyFactory, impl, adminAddress, data));
            break;
        }
    }
    await manifest.addProxy(proxyDeployment);
    Contract.address = proxyDeployment.address;
    Contract.transactionHash = proxyDeployment.txHash;
    const contract = new Contract(proxyDeployment.address);
    contract.transactionHash = proxyDeployment.txHash;
    return contract;
}
exports.deployProxy = deployProxy;
//# sourceMappingURL=deploy-proxy.js.map